[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hybrilink"
version = "0.1.0"
description = "Hybrid public-key + symmetric secure transport (educational prototype)."
requires-python = ">=3.10"
dependencies = ["cryptography>=41.0.0"]
